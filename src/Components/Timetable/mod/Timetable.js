import React from "react";

const TimetableTimings = (params) => {};

const TimetableDays = () => {
  return <div />;
};

const TimetableContent = () => {
  return <div />;
};

const Timetable = () => {
  return (
    <div>
      <TimetableContent />
    </div>
  );
};

export default Timetable;
